# 様々なLLMのSDKの実験場

このディレクトリには、様々なLLMのSDKを使った実験的なコードが含まれています。各サブディレクトリは特定のLLMのSDKに対応しており、そのSDKを使用したエージェントの実装例やツールの利用例が含まれています。

アプリ本体に活かす箇所もあるが、主に学習と実験の目的で作成されています。

実験のしやすさ、アプリとの関わりの現状の薄さを踏まえてフォルダごとに参照することは避け、独立したパッケージとして作成しています。