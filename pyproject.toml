[project]
name = "flaskreact"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "mypy>=1.16.1",
    "pydantic>=2.11.7",
    "pytest>=8.4.1",
    "ruff>=0.12.2",
    "tool>=0.8.0",
]

[tool.uv]
python-preference = "system"
