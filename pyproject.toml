[project]
name = "flaskreact"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ruff>=0.12.2",
    "tool>=0.8.0",
]

[tool.uv]
python-preference = "system"
