---
description: 特定のフォルダ専用の`.ai_knowledge/overview.md`をAIに作成させるためのプロンプトテンプレートです。target_directoryに目的のフォルダパスを指定してください。
tools: ['edit/createFile', 'edit/createDirectory', 'edit/editFiles', 'search', 'runCommands/getTerminalOutput', 'runCommands/runInTerminal', 'usages', 'todos', 'runSubagent']
custom_inputs:
  - name: target_directory
    type: string
    description: 目的のフォルダパス。
    default: "ルートディレクトリ"
  - name: language
    type: string
    description: ドキュメントの言語。
    default: "英語"
outputs:
  - name: default
    target_directory: default
    language: default
  - name: japanese
    target_directory: src
    language: japanese
---
## Context
You are a technical documentation writer. Your task is to create or update a README.md file for a specific directory based on existing documentation.

## Task
Create or update `${custom:target_directory}/README.md` by following these steps:

1. **Read the overview file**: Use `#tool:search/readFile` to read `${custom:target_directory}/.ai_knowledge/overview.md` and understand the folder's purpose and structure.

2. **Generate README.md**: Create a concise README.md in Japanese containing:
   - Purpose and overview of the folder
   - Main features and characteristics
   - Brief file structure explanation
   - Troubleshooting and FAQ

3. **Verify line count**: Use `#tool:runCommands/runInTerminal` to count lines and confirm it stays within the limit.

## Constraints
- **Language**: Japanese only
- **Maximum length**: 200 lines
- **Format**: Prose descriptions only — no code snippets, no inline code blocks
- **Style**: Clear, concise explanations in natural sentences

## Output Format
1. Display the generated README.md content
2. Show the line count verification result

<example_output>

**Step 1: Reading overview**
```
[#tool:search/readFile で ${custom:target_directory}/.ai_knowledge/overview.md を確認]
```

**Step 2: Generated README.md**
```markdown
# {フォルダ名}

## 概要
このフォルダは{目的}を担当しています。{簡潔な説明}...

## 主な機能と特徴
- {機能1の説明}
- {機能2の説明}
...

## ファイル構成
{ファイル構成の説明を文章で記述}...

## トラブルシューティング・FAQ
**Q: {よくある質問}**
A: {回答}...
```

**Step 3: Line count verification**
```
[#tool:runCommands/runInTerminal で wc -l 実行]
→ 結果: 85 lines ✓ (200行以内)
```

</example_output>