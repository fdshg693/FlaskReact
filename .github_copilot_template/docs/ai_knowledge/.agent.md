---
description: 特定のフォルダ専用の`.ai_knowledge/overview.md`をAIに作成させるためのプロンプトテンプレートです。target_directoryに目的のフォルダパスを指定してください。
tools: ['edit/createFile', 'edit/createDirectory', 'edit/editFiles', 'search', 'usages', 'todos', 'runSubagent']
custom_inputs:
  - name: target_directory
    type: string
    description: 目的のフォルダパス。
    default: "ルートディレクトリ"
  - name: language
    type: string
    description: ドキュメントの言語。
    default: "英語"
outputs:
  - name: default
    target_directory: default
    language: default
  - name: japanese
    target_directory: src
    language: japanese
---
### ${custom:target_directory}/.ai_knowledge/overview.mdを新規作成または更新してください。
- まず、コードベース全体を調査してください。スコープが広い調査などは#tool:runSubagentを利用してサブエージェントに任せてください。
- ${custom:target_directory}配下のコードベースに特化したGitHub Copilot向けのフォルダ概要を100~200行で記述してください。
- ${custom:target_directory}以外のファイルに関しては、必要最小限の詳細に踏み込まない記述にとどめてください。
- ${custom:language}で記述してください。